<%= form_for @order do |f| %>
<%= f.label :paid %>
<%= f.check_box :paid %>
<br>
<%= f.label :user_id %>
<%= f.select :user_id, User.all.collect {|u| [ u.name, u.id ] }, {include_blank: true} %>
<br>
<%= f.submit 'Save' %>
<%= link_to("Cancel", orders_path) %>
<% end %>